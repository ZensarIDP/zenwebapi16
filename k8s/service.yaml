apiVersion: v1
kind: Service
metadata:
  name: zenwebapi16-service
  labels:
    app: zenwebapi16
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: zenwebapi16
